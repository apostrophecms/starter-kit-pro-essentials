export default {
  options: {
    // When not in production, refresh the page on restart
    refreshOnRestart: true
    // Change to `apos` for admin UI HMR or `false` to disable.
    // The default is `public` - watch your `ui/src` UI code.
    // hmr: 'apos'
  }
};
